[v] - Fix text clipping in translations and in lyrics.
[v] - Fix getArtistObject Exceptions

[0] - Fix comment loading:
      [v] - Responses
      [v] - datetime
      [v] - Add something to stop when there is no more comments
      [v] - Fix comment replies, causing reply to be the same thing as comment
      [x] - Tidy a little bit that spaghetti # lmao can't do that
      [x] - Add possibility of deleted comment. # TEMP FIX wrap everything in try statement

[ ] - Fix documentation:
      [ ] - Fix classes to contain objects described in documentation

[V] - Add search functionality:
      ### I wanted to create one function to both search for artist and song,
      ### but tekstowo is so dumb that it just gives you two searches on one page.
      [v] - Add functions to find artist or song
      [v] - Create some sort of object holding that data to get normal object

[V] - Add/fix/unify(?) magic methods in models/*
      [v] - Add some magic whenever possible
      ### __repr__    for developer
      ### __str__     for user
      ### __getitem__ for x["asd"]

[ ] - Change classes declarations:
      [ ] - Change classes declaration to not need unneeded variables eg. comment class
      [ ] - Change __init__'s in Lyrics and Artist to accept bs4 object *OR* url

[ ] - Add lots of Exceptions!

[ ] - CLI way of basic interaction

[ ] - Examples?

[?] - Not f*ck everything up.
[0] - Find more bugs!
